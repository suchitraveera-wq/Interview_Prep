# No environment variables are required for the current build.
# Reserved for future use — e.g. if the question sync moves to the
# Sheets API instead of the public CSV export (see scripts/sync-questions.js).
# GOOGLE_SHEETS_API_KEY=
